/*
Create a function that takes an array as an argument and returns an array with all
the pair elements swapped. So the elements with index 0 will swap places with the 
element in index 1, the element at index 2 will swap with 3 and so on.
*/
