/*
What does the following code log to the console?
*/

function scream(words) {
  return words + '!!!!';
}

scream('Yipeee');

//This also logs nothing to the console. The function returns the passed in string
//concatenated with '!!!!', but nothing is done with that value.