/*
Create a function that takes an array as an argument and returns the same 
array with all elements sorted in alphabetical order.
*/

function sorting(array) {
  return array.sort();
}

console.log(sorting(['a', 'd', 'a', 'b']));