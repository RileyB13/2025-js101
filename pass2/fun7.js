/*
Without running the following code, what do you think it will output?
*/

function foo(bar, qux) {
  console.log(bar);
  console.log(qux);
}

foo('Hello');

/*
This will log 'Hello' and then on the following line it will log undefined.
*/
