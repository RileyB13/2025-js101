/*
This one needs added to my list to solidify the concept of variable shadowing.

So the first two snippets do not reassign because of variable shadowing.
The last snippet mutates the arrays with the splice method, so it returns
the mutated objects.
*/
